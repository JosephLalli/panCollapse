Read `AGENTS.md`, `PROGRESS.md`, `TASKS.md`, and the current named-gate definition. State
the current phase, settled constraints, open task, and files that must be read next. Do
not start implementation merely because the repository has no code. Propose one bounded
increment and execute it only if it is permitted by the current gate.
