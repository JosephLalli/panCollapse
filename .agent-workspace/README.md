# Tracked Agent Workspace

This directory contains orchestrator workflow, kickoff, model-selection, and named-gate
material. It is intentionally tracked for the development phase and can be removed later
without deleting the durable product contract in `docs/`.

Use `scripts/remove-agent-workspace.sh --dry-run` to see the removable layer.
